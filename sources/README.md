# sources/psc-kernel is a git submodule: github.com/autobleem2/psc-kernel, branch master (the MT8167
# Linux 4.4.22 vendor fork, with the gcc>=10 build fixes). `git submodule update --init` fetches it.
